# FreeCertify Mobile — Environment Variables Template
# Copy this file to .env and fill in your values.
# All EXPO_PUBLIC_ variables are bundled into the JS build (not secret).
# Get Firebase values from: Firebase Console > Project Settings > Your Apps

EXPO_PUBLIC_FIREBASE_API_KEY=your-api-key
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
EXPO_PUBLIC_FIREBASE_APP_ID=your-app-id
EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Django backend URL
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000

# OAuth: Google (Web client ID required; iOS/Android optional for dev builds)
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=

# OAuth: GitHub — MUST be the same GitHub OAuth App as Firebase Console > Auth > GitHub
# (not a separate "mobile-only" app, or Firebase rejects the access token).
# Register this callback on that GitHub app: freecertify://
# GitHub OAuth Apps allow only ONE callback URL; if web also needs Firebase's handler,
# create a GitHub App (multiple callbacks) and update Firebase with its credentials.
# Secret goes in backend/.env as GITHUB_CLIENT_SECRET. GitHub sign-in also requires
# the Django backend to be running and reachable at EXPO_PUBLIC_API_BASE_URL.
EXPO_PUBLIC_GITHUB_CLIENT_ID=your-firebase-github-client-id
